package com.example.CloneGamestop.Security;

/*import org.springframework.security.core.GrantedAuthority; // Importa l'interfaccia GrantedAuthority per le autorizzazioni

import java.io.Serializable; // Importa Serializable per abilitare la serializzazione

import java.util.Collection;

// Interfaccia per definire i dettagli dell'utente per Spring Security
public interface UserDetails extends Serializable { // Estende Serializable per abilitare la serializzazione

    // Metodo per ottenere le autorizzazioni dell'utente
    Collection<? extends GrantedAuthority> getAuthorities();

    // Metodo per ottenere lo username dell'utente
    String getUsername();

    // Metodo per ottenere la password dell'utente
    String getPassword();

    // Metodo per verificare se l'account dell'utente è scaduto
    boolean isAccountNonExpired();

    // Metodo per verificare se l'account dell'utente è bloccato
    boolean isAccountNonLocked();

    // Metodo per verificare se le credenziali dell'utente sono scadute
    boolean isCredentialsNonExpired();

    // Metodo per verificare se l'account dell'utente è abilitato
    boolean isEnabled();
}*/
